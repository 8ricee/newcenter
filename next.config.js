/** @type {import('next').NextConfig} */

const nextConfig = {
    /* config options here */
    images: {
        domains: [
            'xqwdubchxsiniiy1.public.blob.vercel-storage.com'
        ],
    }
};

module.exports = nextConfig; 